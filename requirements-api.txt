# ============================================================
# 智护银伴 · API-only 轻量依赖清单
# 适用场景：
#   - 受限网络（无法下载 embedding 模型权重）
#   - 无 GPU / 不想装 ~5GB PyTorch
#   - 只用远程 LLM API（LLM_PROVIDER=openai）+ EMBEDDING_DISABLED=true
#
# 用法：
#   pip install -r requirements-api.txt
#   # 或 Docker 构建时：
#   docker build --build-arg PY_REQUIREMENTS=requirements-api.txt .
#
# 与 requirements.txt 的区别：
#   ❌ 不含 torch / transformers / sentence-transformers / huggingface-hub
#   ❌ 不含 pytesseract / pillow（OCR 功能不可用）
#   ✅ 其余功能完全正常（缴费、入院、护理任务卡、PDF 导出、备份等）
# ============================================================

# --- Web 框架 ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- 向量数据库（仍需要，用于档案存储和 patient_id 过滤检索）---
chromadb==0.6.3

# --- HTTP 请求（调用远程 LLM API）---
requests==2.32.3

# --- 数据校验（FastAPI 依赖）---
pydantic==2.10.3

# --- 数据安全 ---
cryptography==43.0.3
python-multipart==0.0.20
python-dotenv==1.0.1
python-dateutil==2.9.0

# --- PDF 导出 ---
reportlab==4.5.1

# --- 日志 & 配置 ---
loguru==0.7.3
pyyaml==6.0.2

# --- 开发 / 测试（可选）---
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.24.0
